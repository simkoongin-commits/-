# 심궁회 개발 규칙

## 작업 방식

- 기능 하나마다 `codex/<기능명>` 브랜치를 만들고 PR로 병합한다.
- 구현 전에는 기존 데이터 모델과 권한 영향을 먼저 확인한다.
- 사용자에게 보이는 동작을 바꾸면 모바일 화면을 우선으로 점검한다.

## 코드 구조

- Firebase에 저장되는 데이터는 TypeScript 타입을 먼저 정의한다.
- 화면 컴포넌트는 화면 표시를, Home 컴포넌트는 공동 상태와 저장을 맡는다.
- 반복되는 날짜·시간·권한 판단은 작은 순수 함수로 분리한다.
- 새 공동 데이터 필드는 읽기, 초기값, 자동 저장, 첫 관리자 초기화에 모두 포함한다.

## 품질 확인

- 변경마다 `npm run build`를 통과시킨다.
- 권한이 있는 기능은 관리자·신사/구사·예비신사 관점에서 확인한다.
- Firebase 규칙 변경이 필요한 경우 코드와 규칙을 함께 반영하고 배포 상태를 확인한다.
