# 심궁회 개발 규칙

## 작업 방식

- 기능 하나마다 `codex/<기능명>` 브랜치를 만들고 PR로 병합한다.
- 구현 전에는 기존 데이터 모델과 권한 영향을 먼저 확인한다.
- 사용자에게 보이는 동작을 바꾸면 모바일 화면을 우선으로 점검한다.

## 코드 구조

- Firebase에 저장되는 데이터는 TypeScript 타입을 먼저 정의한다.
- 화면 컴포넌트는 화면 표시를, Home 컴포넌트는 공동 상태와 저장을 맡는다.
- 반복되는 날짜·시간·권한 판단은 작은 순수 함수로 분리한다.
- 새 공동 데이터 필드는 읽기, 초기값, 자동 저장, 첫 관리자 초기화에 모두 포함한다.
- `app/page.tsx`에는 화면 연결과 공동 상태만 둔다. 새 도메인 규칙은 `lib/`, 독립 화면은 `app/components/`에 둔다.
- 회원 등급·팀·직책 규칙은 `lib/members.ts`를 단일 기준으로 사용한다. 같은 규칙을 화면 안에 다시 작성하지 않는다.
- Firebase 배열 문서를 수정할 때는 화면 상태만 믿지 말고 트랜잭션에서 최신 문서를 다시 검증한다.

## 주요 경로

- `app/page.tsx`: 로그인, 최상위 탭, Firestore 공동 문서 구독과 화면 조합
- `app/components/HeartFive.tsx`: 개인 습사 기록 입력·목록·이미지 저장 UI
- `app/api/heart-five/`: 습사 기록, 포인트, 영구 열람 권한을 처리하는 서버 API
- `app/components/EquipmentManagement.tsx`: 장비 현황 UI. 대여 코드는 유지하지만 현재 화면에서는 숨김
- `lib/members.ts`: 회원 타입, 학기별 등급, 직책별 팀 규칙
- `lib/heartFive.ts`: 시위 값 검증과 습사 통계 계산
- `lib/equipment.ts`: 장비 타입, 정렬·상태·삭제 검증
- `tests/`: 브라우저나 Firebase 없이 검증 가능한 도메인 규칙 테스트

## 데이터 변경 순서

1. `lib/`의 타입과 순수 규칙을 먼저 수정한다.
2. 기존 Firestore 데이터가 새 필드 없이도 읽히도록 기본값을 둔다.
3. 서버에서 보호해야 하는 기능은 API에서 권한과 입력값을 다시 검증한다.
4. UI는 API 또는 최상위 저장 함수만 호출하고 같은 규칙을 중복 구현하지 않는다.
5. 정상·권한 없음·기존 데이터 세 경우를 테스트한다.

## 품질 확인

- 변경마다 `npm test`를 통과시킨다. 빠른 확인은 `npm run build`와 관련 테스트 파일만 실행해도 된다.
- 커밋 전 `npm run lint`와 `git diff --check`로 사용하지 않는 코드와 공백 오류를 확인한다.
- 권한이 있는 기능은 관리자·신사/구사·예비신사 관점에서 확인한다.
- Firebase 규칙 변경이 필요한 경우 코드와 규칙을 함께 반영하고 배포 상태를 확인한다.

## PR 규칙

- 브랜치 이름은 `codex/<짧은-기능명>`으로 만든다.
- PR 설명에는 사용자 동작 변화, 데이터 구조 변화, 실행한 검증을 적는다.
- 이름만 바꾸는 리팩터링과 동작 변경을 한꺼번에 크게 섞지 않는다.
- 삭제한 코드는 Git 기록으로 복구할 수 있으므로 주석 처리한 죽은 코드로 남기지 않는다.
